export const theme = {
  colors: {
    primary: '#6366f1',          // Indigo-500 (brand primary)
    primaryDark: '#4f46e5',      // Slightly darker for hover states
    background: '#ffffff',       // Light background (for light mode)
    foreground: '#111827',       // Rich dark text color
    muted: '#f3f4f6',            // Muted light background
    accent: '#d1d5db',           // Accent shade for micro-elements
  },
  spacing: {
    containerPadding: '1.5rem',  // ~24px consistent container padding
  },
  typography: {
    headingFont: 'Inter, sans-serif',
    bodyFont: 'Inter, sans-serif',
  },
};
